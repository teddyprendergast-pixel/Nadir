# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# JAX / XLA
.jax_cache/

# Training outputs — never commit these.
# NOTE: checkpoints/ and tensorboard_logs/ were committed in f751b86 *before*
# these rules existed, so the rules never applied to them. 169 MB of
# Stable-Baselines3 .zip checkpoints — a framework CLAUDE.md bans — were
# tracked. Removed from HEAD on branch `rob`; still in history.
checkpoints/
runs/
logs/
tensorboard_logs/
wandb/
outputs/
*.onnx
*.msgpack
*.pkl

# Hardware logs (raw system-ID captures live in hardware/measured/raw/, gitignored;
# only the fitted parameters in *.yaml are committed)
hardware/measured/raw/

# Cluster
slurm-*.out
*.sbatch.log

# OS / editor
.DS_Store
.idea/
.vscode/

# Local Claude Code config (machine-specific paths)
.claude/
